#ifndef TYPES_HPP
#define TYPES_HPP

#include <string>
#include <vector>
#include <map>

typedef std::vector<std::string> string_vector;
typedef std::vector<std::vector<std::string> > string_vectors;
typedef std::map<std::string, std::string> string_map;

#endif
